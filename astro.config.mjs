import {defineConfig} from 'astro/config';
export default defineConfig({site:'https://marketing.parkskazka.ru',base:'/invest-day-2026',output:'static',trailingSlash:'always',build:{inlineStylesheets:'never'}});
